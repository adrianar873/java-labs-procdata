package mongodb;


public class CheckingMongo {
    public static void main(String[] args) {

    	//Inspection.searchByCertificateNumber(9278806);
    	
    	
    	
//        // Cargar variables de entorno
//        Dotenv dotenv = Dotenv.load();
//        String password = dotenv.get("DB_Password");
//
//        // URI de conexión
//        String uri = "mongodb+srv://adrianar:" + password + "@cluster0.hgcsubk.mongodb.net/?retryWrites=true&w=majority";
//
//        // Conexión a MongoDB
//        try (MongoClient mongoClient = MongoClients.create(uri)) {
//
//            // Seleccionar la base de datos y colección
//            MongoDatabase database = mongoClient.getDatabase("sample_training"); 
//            MongoCollection<Document> collection = database.getCollection("trips");
//
//            // Buscar los viajes con tripduration < 500
//            FindIterable<Document> trips = collection.find(Filters.lt("tripduration", 500));
//
//            // Imprimir resultados
//            for (Document trip : trips) {
//                System.out.println(trip.toJson());
//            }
//
//        } catch (Exception e) {
//            e.printStackTrace();
//        }
    	
    	
    	
    	
    	
    }
}
